* Start Tyk with the API Bridge Agnt plugin: `make start_tyk`
* Install the [UV](https://docs.astral.sh/uv/) python package manager (`brew install uv` for example)
* Run the example with `uv run main.py`
